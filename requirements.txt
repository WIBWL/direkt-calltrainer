# Web / API server
fastapi==0.109.0
gunicorn==21.2.0
httpx==0.28.1
pyjwt[crypto]==2.13.0
python-dotenv==1.0.1
uvicorn[standard]==0.27.0
# UploadFile, the PDF upload in /api/scenarios/document
python-multipart==0.0.32

# AI pipeline clients (STT / LLM / TTS)
kugelaudio==1.9.0
openai==1.109.1

# Scenario authoring: pull text out of an uploaded PDF (text-layer only, no OCR)
pypdf==5.1.0

# Database: Postgres driver and migrations
alembic==1.14.0
psycopg[binary]==3.2.10
sqlalchemy==2.0.36

# Post-call feedback: Praat measurement and the async wrap-up worker
numpy==2.5.2
praat-parselmouth==0.4.7
redis==8.1.0
rq==2.12.0
