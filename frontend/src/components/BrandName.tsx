// The wordmark lives here alone so the highlighted "ai" cannot drift between
// the header, the login screen and the redirect card.
export default function BrandName() {
  return (
    <>
      Calltr<span className="brand-ai">ai</span>ner
    </>
  );
}
